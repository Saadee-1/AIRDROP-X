# AIRDROP-X runtime (CLI and Qt desktop app)
numpy>=1.20
matplotlib>=3.5
PyQt6>=6.4

# Web preview (browser + desktop embed)
streamlit>=1.28.0
pywebview>=4.0

# Optional: for building Windows EXE
# pyinstaller>=6.0
